<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <UseAppHost>true</UseAppHost>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
  </PropertyGroup>
  <ItemGroup>
    <Compile Include="..\ProcessPipe.cs" Link="ProcessPipe.cs" />
  </ItemGroup>
  <Target Name="GetCorruptionFiles" DependsOnTargets="Build" Returns="@(CorruptionFiles)">
    <ItemGroup>
      <CorruptionFiles Include="$(TargetDir)$(AssemblyName).dll;$(TargetDir)$(AssemblyName).deps.json;$(TargetDir)$(AssemblyName).runtimeconfig.json" />
      <CorruptionFiles Include="$(TargetDir)$(AssemblyName).exe" Condition="$([MSBuild]::IsOSPlatform('Windows'))" />
      <CorruptionFiles Include="$(TargetDir)$(AssemblyName)" Condition="!$([MSBuild]::IsOSPlatform('Windows'))" />
    </ItemGroup>
  </Target>
</Project>
