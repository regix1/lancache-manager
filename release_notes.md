What's New

Translation support

The UI now supports translations across the app, with updated strings and language handling.
Prefill pages and status messages were moved onto the translation system.
Additional translation tweaks and copy fixes landed across the interface.

Prefill and Docker updates

Docker improvements for prefill and general container behavior.
Prefill defaults now accept auto for network, DNS, host data path, and TCP mode.
Build fixes to keep the pipeline clean.

Calendar and UI polish

Calendar fixes for more reliable navigation and rendering.
Mobile layout updates plus tooltip and popover improvements.
Small UI cleanup in cards, spacing, and menus.

Documentation

README updates and small corrections in the configuration guide.

Thanks for using LANCache Manager!
